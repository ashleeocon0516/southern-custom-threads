SCT WEBSITE FIXED PACKAGE

This package uses the ORIGINAL 14 master gang sheets as the source.
It does NOT replace them with tiny individual image files.

UPLOAD:
1. Extract this ZIP.
2. Open the extracted folder.
3. Select ALL files inside it.
4. Upload those files to the ROOT of the southern-custom-threads GitHub repository.
5. Commit the changes.
6. Wait for GitHub Pages to rebuild.

The site code references the existing SCT logo already in the repository, so the logo does not need to be re-uploaded.

The 14 master sheets are copied unchanged.
